import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  // Optional: Set basePath if deploying to a subdirectory
  // basePath: '/wow_landing',
};

export default nextConfig;
